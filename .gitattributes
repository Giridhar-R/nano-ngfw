# Normalise to LF in the repository. CI builds on Linux, and pcap fixtures are
# generated by a Python script that must not be mangled by line-ending rewrites.
* text=auto eol=lf

# Captures are binary and must never be touched.
*.pcap  binary
*.pcapng binary
